export const getRoom = (state) => {
  return state.room
}

export const getMeName = (state) => {
    return state.state.name
}

export const getState = (state) => {
  return state.state
}

export const getClients = (state) => {
  return state.clients
}